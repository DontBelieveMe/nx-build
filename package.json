{
    "name": "nx-build",
    "version": "0.0.1",
    "scripts": {
        "test": "mocha tests",
        "cover": "istanbul cover --include-all-sources node_modules/mocha/bin/_mocha tests/",
        "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
    },
    "dependencies": {
        "coveralls": "^3.0.0",
        "istanbul": "^0.4.5",
        "mocha": "^5.0.5",
        "mocha-lcov-reporter": "^1.3.0"
    }
}
